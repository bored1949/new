#include <bits/stdc++.h>
using namespace std;
#define ll long long int
void sieve(int n)
{
    vector <int> pf[n+1];
    for(int i=1; i<=n; i++)
    {
        for(int j=i; j<=n; j+=i)
        {
            pf[j].push_back(i);
        }
    }
}
int main()
{
    int n;
    cin >> n;
    sieve(n);
}
